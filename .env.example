# =============================================================
# .env.example
# Copia este archivo como ".env" (en la misma carpeta que
# docker-compose.yml) y reemplaza los valores reales antes de
# levantar el proyecto. El archivo ".env" NO debe subirse al
# repositorio (agrégalo a .gitignore).
# =============================================================

DB_NAME=restaurante
DB_PASSWORD=CambiaEstaContraseñaPorUnaSegura
JWT_SECRET=happier71560264
ADMIN_SECRET_CODE=PANCHITA2026
